# --- ядро (шаги 1-6, работает уже сейчас)
streamlit>=1.37
psycopg2-binary>=2.9
requests>=2.31

# --- секреты: чтение .streamlit/secrets.toml из cron-скриптов
# (py3.11+ имеет tomllib в стандартной библиотеке; toml — фолбэк для 3.10)
toml>=0.10

# --- шаги 7-9 и дальше (задел наперёд)
pandas>=2.0          # таблицы каталога, экспорт CSV «Исправить все»
anthropic>=0.34      # официальный SDK — переедем с raw requests в analyze/synthesis
python-dotenv>=1.0   # .env для локальной разработки без secrets.toml
tenacity>=8.2        # ретраи декоратором вместо ручных циклов (рефактор)

# --- интеграции ближнего беклога
gspread>=6.0         # выгрузки в Google Sheets (паттерн как в CA-003)
google-auth>=2.0     # авторизация для gspread
openpyxl>=3.1        # экспорт xlsx для флэтфайлов Amazon

# --- закомментировано до реальной надобности, чтобы не таскать лишнее:
# keepa>=1.3         # keyword data для обхода Cerebro (беклог page-1 gate)
# plotly>=5.0        # графики До/после, если st.line_chart перестанет хватать
